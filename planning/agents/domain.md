# Yii architecture boundary

Yii owns configuration-provider composition, DI bindings, HTTP and console entry points, Twig presentation, and future adapters. Shared Fight packages are Composer dependencies only.