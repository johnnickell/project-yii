services:
  app:
    build: .
    working_dir: /app
    volumes:
      - .:/app
    command: php-fpm

  web:
    image: nginx:1.29-alpine
    depends_on:
      - app
    ports:
      - "8088:80"
    volumes:
      - .:/app:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
